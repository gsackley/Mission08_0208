@{
    ViewData["Title"] = "Quadrants";
}

@model List<Task>

<table class="table table-bordered table-striped">
    <thead class="font-weight-bold">
        <tr>
            <td>Urgent and Important</td>
            <td>Urgent and Not Important</td>
            <td>Not Urgent and Important</td>
            <td>Not Urgent and Not Important</td>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
            {

                    @if (item.Quadrant == 1)
                {
                    <tr>
                        <td>
                            @item.Category
                            @item.TaskName
                            @item.DueDate
                            @item.Completed
                        </td>
                        <td></td>
                        <td></td>
                        <td></td>

                        <td>
                            <a asp-controller="Home" asp-action="Edit" asp-route-applicationid="@item.TaskId">
                                Edit
                            </a>
                        </td>
                        <td>
                            <a asp-controller="Home" asp-action="Delete" asp-route-applicationid="@item.TaskId">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
                    @if (item.Quadrant == 2)
                {
                    <tr>
                        <td></td>
                        <td>
                            @item.Category
                            @item.TaskName
                            @item.DueDate
                            @item.Completed
                        </td>
                        <td></td>
                        <td></td>

                        <td>
                            <a asp-controller="Home" asp-action="Edit" asp-route-applicationid="@item.TaskId">
                                Edit
                            </a>
                        </td>
                        <td>
                            <a asp-controller="Home" asp-action="Delete" asp-route-applicationid="@item.TaskId">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
                    @if (item.Quadrant == 3)
                {
                    <tr>
                        <td></td>
                        <td></td>
                        <td>
                            @item.Category
                            @item.TaskName
                            @item.DueDate
                            @item.Completed
                        </td>

                        <td>
                            <a asp-controller="Home" asp-action="Edit" asp-route-applicationid="@item.TaskId">
                                Edit
                            </a>
                        </td>
                        <td>
                            <a asp-controller="Home" asp-action="Delete" asp-route-applicationid="@item.TaskId">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
                    @if (item.Quadrant == 4)
                {
                    <tr>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td>
                            @item.Category
                            @item.TaskName
                            @item.DueDate
                            @item.Completed
                        </td>

                        <td>
                            <a asp-controller="Home" asp-action="Edit" asp-route-applicationid="@item.TaskId">
                                Edit
                            </a>
                        </td>
                        <td>
                            <a asp-controller="Home" asp-action="Delete" asp-route-applicationid="@item.TaskId">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            }

    </tbody>
</table>